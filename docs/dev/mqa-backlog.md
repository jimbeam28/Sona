# mqa-backlog — 待真机清单（攒单制）

> SCHEMA §3.3：MQA 不阻塞 impl_status=done。manual_qa_required 条目的验证项追加到此，用户下次装真机时一次性跑完勾选。

## BUG-27 播放器健壮性（PLY4+PLY5）（追加于 2026-08-05）
- □ 4G 弱网（或限速代理）下播放队列中唯一曲目，待加载明显变慢后快速移除该曲目使队列清空 — 期望：播放立即停止，无残余声音、被删曲目不再重新起播（ghost playback 不发生）
- □ 正常播放中多次连续快速移除最后一曲（重复制造空队列） — 期望：每一轮播放器都正确停止、队列清空，任一轮均不出现 ghost playback（覆盖 beginRequest 后 gate 任务已越过 isLatest 检查的极端竞态窗口）

## BUG-01 通知栏/耳机 skip 必抛 TypeError（追加于 2026-08-16）
- □ BUG-01-MAN1：真机播放任意曲目后下拉通知栏按"下一首" — 期望：切到下一曲，`adb logcat` 无 `TypeError` / `Null check` 字样（修复前每次按键平台侧记一次 TypeError）
- □ BUG-01-MAN2：真机插耳机，播放中连按线控"下一首/上一首"各 3 次 — 期望：每次均正常切歌，无异常日志
- □ BUG-01-MAN3：真机锁屏界面点"上一首/下一首" — 期望：切歌正常

## BUG-02 skip 回调绑定 PlayerScreen dispose（追加于 2026-08-16）
- □ BUG-02-MAN1：真机播放 → 按返回键退出播放页回浏览页（音乐继续后台播）→ 通知栏按"下一首" — 期望：切到下一曲（修复前按钮无响应）
- □ BUG-02-MAN2：真机播放中退到桌面 → 锁屏/通知栏按"上一首、下一首"各 2 次 — 期望：均正常切歌
- □ BUG-02-MAN3：真机播放中进设置页再返回（不经播放页）→ 通知栏"下一首" — 期望：仍有效
- □ BUG-02-MAN4：真机冷启动后未播放，检查 logcat — 期望：无崩溃、无异常日志（接线已存在但空队列无副作用）

## BUG-09 嵌套 PopScope 返回键双重动作（追加于 2026-08-16）
- □ BUG-09-MAN1：真机浏览器进 ≥2 层子目录，按系统返回键/手势返回各 3 次 — 期望：每次仅逐级回退一级目录，应用保持前台不最小化（修复前目录回退 + 应用退后台）
- □ BUG-09-MAN2：真机浏览器根目录按返回 → 应用最小化、音频经前台服务继续播放；播放单 Tab 按返回同样最小化且目录不被改动
- □ BUG-09-MAN3：真机 Android 13+（预测性返回）子目录快速连按返回 3 次 — 期望：目录逐级回退且应用不退后台；Tab 切换动画中按返回不崩溃、不误动作

## REF-09 数据层反向依赖解耦（追加于 2026-08-16）
- □ 真机播放一首歌，锁屏/通知栏标题显示文件名去扩展名（extractTitleFromPath 路径回归）
- □ 播放进度 30s 后杀进程再开，恢复进度提示正常出现（progress_policy 路径回归）

## BUG-20 退出播放页后自动保存/暂停保存监听被取消（追加于 2026-08-23）
- □ BUG-20-MAN1：真机播长章节 → 进出全屏播放页 → 后台收听 5 分钟 → 通知栏暂停 → 杀进程 → 重开 — 期望：恢复到暂停处（后台区间不丢失）

## BUG-21 末曲播完缺 seek(0) 滞留 completed 态（追加于 2026-08-23）
- □ BUG-21-MAN1：真机单曲播完 → 不按播放键 → 直接拖进度条 — 期望：松手后位置生效且可暂停（修复前滑条冻结）

## TMR-01 定时停止音量淡出（追加于 2026-08-24）
- □ TMR-01-MAN1：真机播放中设 1 分钟定时并锁屏 — 期望：最后 10 秒音量平滑渐弱至无声后暂停（锁屏/Doze 下 Flutter 秒级 tick 可能被 OS 推迟，若出现跳变或"直接停"需回填平台踩坑库）
- □ TMR-01-MAN2：真机扬声器主观听感验收 10s 线性曲线 — 期望：平缓不突兀；过陡/过缓反馈回填 kTimerFadeWindow 或曲线参数
- □ TMR-01-MAN3：真机蓝牙耳机播放设 2 分钟定时 — 期望：蓝牙上淡出生效（setVolume 与系统音量/蓝牙绝对音量协议相互作用无法模拟）
- □ TMR-01-MAN4：真机最后 10 秒内取消定时器/重新设更长定时 — 期望：最迟约 1 秒音量恢复正常，后续播放音量正常

## SRCH-01 文件搜索（追加于 2026-08-24）
- □ SRCH-01-MAN1：真实 NAS 上对 200 目录规模子树发起搜索 — 评估扫描耗时与体验（逐层 RTT 叠加无法在 fake 环境模拟；若不可接受需考虑并发层/提前截断提示，回 dev-plan）

## DL-01 离线下载（追加于 2026-08-25，spec §8 真机风险列）
- □ DL-01-MAN1：真机下载单曲/文件夹后开飞行模式播放已下载曲目 — 期望：秒开不走网络，锁屏控件一切如常（U5；AudioSource.file 真机解码路径 fake 测不到）
- □ DL-01-MAN2：真机切后台/Doze 冻结 30 分钟观察长下载存活率 — 期望：记录中断行为（无前台 Service，Dart 定时器冻结即停属已知裁决 B5-9；若冻结后泵卡死不恢复需回填平台踩坑库）
- □ DL-01-MAN3：真机剩余空间约 1GB 时下载 2GB 目录 — 期望：逐条失败标 failed、.part 清理、App 不崩（IOSink 写满磁盘的真实 errno 路径无法注入）
- □ DL-01-MAN4：真机弱网（限速代理）下载大 FLAC — 观察 chunk 间 30s 静默误判率（极慢但活着的链路），必要时调 kChunkIdleTimeout 常量回 dev-plan
- □ DL-01-MAN5：群晖/威联通实测长连接 GET 中途掐断失败率 — 期望：失败条目标 failed 可重试（TCP 半开 fake 无法模拟）
- □ DL-01-MAN6：本地文件被系统清理（或手动删应用私有目录文件）后再播该曲目 — 期望：静默回退在线流式，管理页该条变失败可重试（U6 resolver existsSync 分支真机验证）
