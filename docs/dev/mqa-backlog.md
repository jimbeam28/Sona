# mqa-backlog — 待真机清单（攒单制）

> SCHEMA §3.3：MQA 不阻塞 impl_status=done。manual_qa_required 条目的验证项追加到此，用户下次装真机时一次性跑完勾选。

## BUG-27 播放器健壮性（PLY4+PLY5）（追加于 2026-08-05）
- □ 4G 弱网（或限速代理）下播放队列中唯一曲目，待加载明显变慢后快速移除该曲目使队列清空 — 期望：播放立即停止，无残余声音、被删曲目不再重新起播（ghost playback 不发生）
- □ 正常播放中多次连续快速移除最后一曲（重复制造空队列） — 期望：每一轮播放器都正确停止、队列清空，任一轮均不出现 ghost playback（覆盖 beginRequest 后 gate 任务已越过 isLatest 检查的极端竞态窗口）

## BUG-01 通知栏/耳机 skip 必抛 TypeError（追加于 2026-08-16）
- □ BUG-01-MAN1：真机播放任意曲目后下拉通知栏按"下一首" — 期望：切到下一曲，`adb logcat` 无 `TypeError` / `Null check` 字样（修复前每次按键平台侧记一次 TypeError）
- □ BUG-01-MAN2：真机插耳机，播放中连按线控"下一首/上一首"各 3 次 — 期望：每次均正常切歌，无异常日志
- □ BUG-01-MAN3：真机锁屏界面点"上一首/下一首" — 期望：切歌正常

## BUG-02 skip 回调绑定 PlayerScreen dispose（追加于 2026-08-16）
- □ BUG-02-MAN1：真机播放 → 按返回键退出播放页回浏览页（音乐继续后台播）→ 通知栏按"下一首" — 期望：切到下一曲（修复前按钮无响应）
- □ BUG-02-MAN2：真机播放中退到桌面 → 锁屏/通知栏按"上一首、下一首"各 2 次 — 期望：均正常切歌
- □ BUG-02-MAN3：真机播放中进设置页再返回（不经播放页）→ 通知栏"下一首" — 期望：仍有效
- □ BUG-02-MAN4：真机冷启动后未播放，检查 logcat — 期望：无崩溃、无异常日志（接线已存在但空队列无副作用）

## BUG-09 嵌套 PopScope 返回键双重动作（追加于 2026-08-16）
- □ BUG-09-MAN1：真机浏览器进 ≥2 层子目录，按系统返回键/手势返回各 3 次 — 期望：每次仅逐级回退一级目录，应用保持前台不最小化（修复前目录回退 + 应用退后台）
- □ BUG-09-MAN2：真机浏览器根目录按返回 → 应用最小化、音频经前台服务继续播放；播放单 Tab 按返回同样最小化且目录不被改动
- □ BUG-09-MAN3：真机 Android 13+（预测性返回）子目录快速连按返回 3 次 — 期望：目录逐级回退且应用不退后台；Tab 切换动画中按返回不崩溃、不误动作

## REF-09 数据层反向依赖解耦（追加于 2026-08-16）
- □ 真机播放一首歌，锁屏/通知栏标题显示文件名去扩展名（extractTitleFromPath 路径回归）
- □ 播放进度 30s 后杀进程再开，恢复进度提示正常出现（progress_policy 路径回归）
