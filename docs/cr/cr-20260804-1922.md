# CR 修复复核报告

> 生成时间：2026-08-04 19:22
> 复核范围：cr-20260724-0110 / cr-2026-06-28 走查出、并经 26 个修复提交（9cd9ce2..8c58a08）修复的全部条目
> 复核方式：25 个审查单元逐项串行派发独立 agent，每项核对「实现 vs spec §3.1 修改指令 + CR 原始问题 + 门禁测试非空壳性（两态实证 / mutation 验证）」，并跑模块/全量测试
> 问题总数：**Critical 1 / Major 8 / Minor 6 / TEST-GAP 10**（21 项发现问题，已随复核修正；4 项原样合格）
> 复核产出：21 个复核修正提交（ef3305f..ef3d386），新增门禁/回归测试 26 个文件，测试 1713 → 1980 全绿，main 与 origin/main 同步

## 摘要

对「CR 走查 → 修复」链条的修复质量复核发现：**25 项中 21 项的修复存在问题**。问题分三类：

1. **修复实现缺陷**（最严重）：修复偏离 spec 语义、spec 只落地一半、甚至修复对目标场景实际无效。典型：PLY3「排列耗尽重洗新一轮」用户裁决整体未实现（Critical）；BUG-22 焦点映射错误导致通话结束后无法恢复播放；BUG-21 的 `ref.read`-in-`onDispose` 模式在 container dispose 路径根本不执行；BUG-16 实证修复前 FK **从未开启过**；BUG-24/25 的 `context.mounted` 检查在 defunct State 上本身即抛。
2. **门禁测试系统性缺失**：25 项中 18 项的 spec §5.4 指定门禁测试从未落地，dev-exe 只改 `lib/` 即标 done；既有用例对修复零锚定（回退修复照样全绿），「修复前 FAIL → 修复后 PASS」链条断裂。
3. **流程红线**：BUG-32 原修复为迁就偏差实现**改写了既有测试断言**（BUG-07-T03，已恢复）；BUG-21 spec 修复模式本身有 Riverpod 机制缺陷、BUG-25-S4/S5 spec 方案在真实 Flutter 语义下不可行——dev-plan 未验证方案可行性。

所有发现问题已由复核 agent 直接修复并推送（21 个提交，均经 pre-push 全量门禁）。另有 10 项范围外问题待裁决，见 §4。

---

## §1 复核结论总表（25 项）

| # | 审查单元 | 原修复提交 | 结论 | 严重度 | 复核修正 |
|---|---------|-----------|------|--------|---------|
| 1 | PRG1/PRG2/BRW 进度簇 | 9cd9ce2 等 | ✅ 合格 | — | — |
| 2 | NET1 basePath 同源 | ba206e8 | ✅ 合格（遗留数据迁移另见 O1） | — | — |
| 3 | CON2/NET7 凭证防护 | 5e6eb1e | 🔧 脱敏正则多 @ 泄漏 | Major | ef3305f |
| 4 | CON1 dispose 后失败 | cf1010e | 🔧 静默吞错无日志 | Minor | d0f43c4 |
| 5 | CON3 编辑连接清缓存 | 5e6eb1e | ✅ 合格（顺带证实 CON4 已在 596a63b 落地且正确） | — | — |
| 6 | BUG-30 modifiedAt | 7e05166 | ✅ 合格 | — | — |
| 7 | BUG-16 FK PRAGMA | 32853c2 | 🔧 S2+门禁缺失；实证修复前 FK 从未开启 | Major | 720de22 |
| 8 | BUG-15 href 识别 | 8d71b72 | 🔧 门禁缺失 | TEST-GAP | f1ce67f |
| 9 | BUG-28 setSeekStep | 7cc3b05 | 🔧 门禁缺失（既有用例零锚定运行时 provider） | TEST-GAP | 9e8a4c6 |
| 10 | BUG-20 短文件清除窗口 | 67025ad | 🔧 门禁缺失（§5.3 边界用例集零覆盖） | TEST-GAP | cd181dd |
| 11 | BUG-11+12 XML 实体/URL | 4af0fe5 | 🔧 门禁缺失 | TEST-GAP | 2555c35 |
| 12 | BUG-05+17 超时兜底 | ca46bc3 | 🔧 seek 失败阻塞 play（违反 spec 否定断言）+ 门禁缺失 | Major | cbb3098 |
| 13 | BUG-07 Tab 排序菜单 | 769f846 | 🔧 门禁缺失 | TEST-GAP | 67e810b |
| 14 | BUG-09 跨目录全选 | 4f50954 | 🔧 门禁缺失 | TEST-GAP | fd8e69a |
| 15 | BUG-13 从头播放删进度 | d4856ec | 🔧 门禁缺失（删除副作用零断言） | TEST-GAP | a9221c6 |
| 16 | BUG-06 下一曲响应 | b9dbeb7 | 🔧 门禁缺失（响应性零覆盖） | TEST-GAP | 897d356 |
| 17 | BUG-19 saveProgress | 84acb3f | 🔧 catchError 静默吞错无日志 + 门禁缺失 | Minor | 0607ee3 |
| 18 | BUG-21 onDispose 清理 | b75b7ba | 🔧 **spec 模式本身在 container dispose 路径无效** | Major | ba73f85 |
| 19 | BUG-26 DAO 簇 | d5da6c0 | 🔧 S4 clock 注入整体缺失 + 窄捕获偏离 | Minor | 418701d |
| 20 | BUG-29 定时器显示 | b1a4831 | 🔧 门禁缺失（既有用例全 noop override） | TEST-GAP | a89d61b |
| 21 | BUG-32 服务层容错 | 8c58a08 | 🔧 空转 rethrow + 超时误导文案 + **篡改既有断言** | Major | 08fd938 |
| 22 | BUG-23 网络簇 | 193ef56 | 🔧 S3 词边界 + S4 状态码分离完全缺失 | Major | 58d4f11 |
| 23 | BUG-04+14 shuffle | 596a63b | 🔧 **PLY3 重洗（用户裁决）未实现** + 恢复路径潜伏缺陷 | **Critical** + Minor | 17a9010 |
| 24 | BUG-18+22+31 | ee7d1e0 | 🔧 焦点映射错误致通话后无法恢复 + 超时偏离 + 时钟注入漏做 | Major×2 + Minor | 2f946ff |
| 25 | BUG-24+25 | 3d6bc26（含 596a63b 夹带） | 🔧 spec 仅落地约 1/3，多处修复对目标场景无效 | Major | ef3d386 |

---

## §2 修复实现缺陷清单（按严重度）

> 证据格式：`缺陷证据`（原修复的问题）→ `修正锚点`（当前代码已修正位置）→ `门禁`（两态实证结果）。

### Critical

#### C1. PLY3「排列耗尽重洗新一轮」用户裁决整体未实现（BUG-04-S2/S3）

- 类型 / 严重度 / 维度：修复缺失 / **Critical** / 功能-用户裁决
- 原修复：596a63b
- 缺陷证据：
  - 用户裁决（cr-20260724-0110 PLY3 分流）明确「重洗新一轮」；596a63b **未触碰 `playback_orchestrator.dart`**，排列耗尽/到达头部仍降级 `nextIndex`/`previousIndex` 随机盲选（可重复、可跳空）。
  - 附带缺陷 1（BUG-04-S4）：`withIndex` 在 `newIndex` 不在排列中时保留旧 `shufflePosition`——`insertAfterCurrent` 排除的插入曲可达，指针悬挂。
  - 附带缺陷 2（BUG-14 健壮性）：`fromMap` OOB 过滤缩短排列后 `shufflePosition` 可越界，`retreatShuffle` 将 RangeError。
  - 附带缺陷 3（BUG-14，Minor）：`restoreQueueFromPrefsProvider` 同步前缀在自身 build 期写 `currentPlayQueueProvider`，Riverpod 断言使恢复在 debug 构建整体降级 AsyncError——持久化队列（含 shuffle 状态）实际读不回。
- 修正锚点：
  - `lib/features/player/domain/playback_orchestrator.dart:278,312,461,485-489` 新增 `_regenerateShuffleQueue`（排除刚播完曲目作首曲；forPrevious 指针落末尾），接入 `computeNextQueue`/`skipToNext`/`skipToPrevious`。
  - `lib/shared/models/play_queue.dart:80-81` 暴露 `generateShuffleOrder` 公开包装；`:118-132` `withIndex` 不在排列降级排列末尾；`fromMap` 位置归一（当前曲在排列→重定位，否则末尾；空排列→0；无排列→null）。
  - `lib/features/browser/browser_provider.dart:163` body 首部 `await null` 让出 build 期。
- 门禁：`test/features/player/bug_bug04_fixed_test.dart`（S1-S4 + INV1-3 + ALG1/2，含否定断言与 mutation 锚定）+ `test/features/browser/bug_14_repro_test.dart`（S1/S2 + INV1/2 + fromMap OOB 五态）。596a63b 零新增测试；双态实证修复前 FAIL。

### Major

#### M1. CON2/NET7 日志脱敏对「密码含裸 @」泄漏 + userInfo 专属文案丢失

- 类型 / 严重度 / 维度：安全-凭证泄漏 / Major / 安全
- 原修复：5e6eb1e
- 缺陷证据：authority 含多个 `@`（如 `http://admin:p@ss@host`）时：
  1. `redactUrlForLog` 正则 `(://)[^/?#@]+@` 只剥到**首个** `@`，日志残留密码尾部（`http://ss@host…`），与自身文档「纵深防御不得泄漏凭证」相悖。
  2. Dart `Uri.tryParse` 对多 @ authority 直接返回 null，`validateUrl` 的 userInfo 分支被跳过，变体只落通用报错，丢失「请把凭证填到用户名/密码栏」专属指引（拒绝本身无绕过路径，属文案归位）。
- 修正锚点：
  - `lib/core/network/webdav_client.dart:107-108` 正则改 `(://)[^/?#]*@` 贪婪剥到 authority 内最后一个 `@`（`/ ? #` 界定 authority，path/query 中 `@` 不误伤）；`:88-98` 文档注明复核修正。
  - `validateUrl` 改用 `redactUrlForLog(s) != s` 作探测（与日志脱敏单源同一 pattern）。
- 门禁：`test/features/browser/bug_14_repro_test.dart` 新增 4 条回归（validate 专属文案 + redact 单元/异常文本/validate 入口日志三层全剥断言），先 FAIL 后 PASS；既有断言零改动。

#### M2. BUG-16 修复前 FK 从未开启；S2 测试库单源化缺失

- 类型 / 严重度 / 维度：修复不完整 / Major / 数据一致性
- 原修复：32853c2（只落了 S1 生产 `onConfigure`）
- 缺陷证据：
  - 实证：sqflite 的 `onCreate` 跑在事务内，事务内 `PRAGMA foreign_keys` 是 **no-op**——修复前 FK 从未开启过，首次安装即失效，CASCADE 删除一直靠应用层自律。
  - 测试库走另一套打开路径且硬编码 PRAGMA，`progress`/`connections` schema 无 FK，掩盖生产行为；S2（测试库与生产同机制）与 §5.4 门禁均缺失。
- 修正锚点：
  - 生产：`lib/core/database/database_helper.dart:29,33-34`（S1，原修复已落）；`:112` 新增 `resetForTest`。
  - 测试库单源化：`test/helpers/test_database.dart:86,98-99` PRAGMA 移入 `onConfigure`（对所有 schema 生效），删 playlist/full 分支硬编码；新增 `seedConnection`（FK 开启后 progress 测试须先建父连接行）。
- 门禁：`bug_16_repro_test.dart` 追加 S1 端到端——文件库 close 后重开（版本匹配不触发 onCreate，精确模拟重启），断言 `PRAGMA foreign_keys=1` + 级联删除 + 孤儿行否定断言；S2 断言孤儿 progress 行被拒。**门禁在修复前代码上失败**。受影响 setUp 补 seedConnection（prg×4、ref_25、bug_bug03、bug_11、brw_04、aud_03-T06），断言一律未动。

#### M3. BUG-05 completed 恢复 seek 失败/超时阻塞 play（违反 spec 否定断言）

- 类型 / 严重度 / 维度：语义偏离 / Major / 功能-平台坑回归
- 原修复：ca46bc3
- 缺陷证据：`audio_handler.play()` 内 completed 恢复的 `seek(Duration.zero)` 与 `play()` 共用一个 try/catch——seek 抛错或 5s 超时时 catch 吞掉后**直接跳过 `_player.play()`**。违反 spec §3.1 否定断言「seek 失败不阻塞 play 调用（try/catch 包裹 seek）」。seek 挂起正是 P4/BUG-17 记录的平台坑，该偏离会在 completed 态经后台路径**复现原始卡死**。
- 修正锚点：`lib/core/services/audio_handler.dart:226-238` 恢复 seek 套内层 try/catch，保留 5s 超时（BUG-17-INV1），失败/超时后仍尝试 play。BUG-17 侧（seek/setSpeed 超时保护 `:266,273`）原修复忠实，未动。
- 门禁：`bug_05_handler_play_test.dart` + `bug_17_repro_test.dart` 共 18 用例（completed 态 stream 注入 → seek 先于 play verifyInOrder、非 completed 不 seek、seek 抛错/挂起均不阻塞 play、永不返回 mock 的六方法超时扫描）。两份 spec §5.4 门禁此前均未落地。双态实测：修复前 S1-T03/T04 FAIL。

#### M4. BUG-21 spec 修复模式本身无效——`ref.read`-in-`onDispose` 在 container dispose 路径不执行

- 类型 / 严重度 / 维度：修复无效（spec 模式缺陷） / Major / 资源泄漏
- 原修复：b75b7ba
- 缺陷证据：原修复在 `onDispose` 内用 `ref.read(state)?.cancel()`。Riverpod 2.6 的 `ProviderContainer.dispose()` **先置 `_disposed=true` 再逐个 dispose element**，onDispose 内 `ref.read` 抛 StateError 且被 `runGuarded` 吞掉上报 zone——cancel 根本不执行。ProviderScope dispose 路径（CR F7 的核心场景）Timer/订阅泄漏照旧，修复对目标场景零效果。
- 修正锚点：`lib/features/player/player_provider.dart:205-209,222-224,246-248` 三个 start provider 改闭包局部句柄取消（dispose 时零容器访问）；镜像 StateProvider 保留（显式 cancel provider 仍消费）。`startProcessingListenerProvider` 同缺陷一并修正。
- 门禁：`bug_bug21_repro_test.dart`——S1/S2 正常路径不变 + container dispose 后不再触发保存（否定断言）+ INV1 源码扫描（三个 start provider 均有 onDispose 且禁 `ref.read`）。**变异验证：还原破损模式 S1-T02 确实失败**。

#### M5. BUG-32 空转 rethrow 偏离裁决 + 超时误导文案 + 篡改既有测试断言（流程红线）

- 类型 / 严重度 / 维度：语义偏离 + 流程红线 / Major / 容错纪律
- 原修复：8c58a08
- 缺陷证据：
  1. `audio_source_builder` preload 密码读超时按 spec 裁决应 catch+debugPrint+跳过，原修复 rethrow——调用方无人 catch，异常空转。
  2. `connection_provider` 启动校验的密码读超时误报 `networkError()`，banner 误导为「网络不可达」。
  3. **流程红线：8c58a08 改写了既有测试 BUG-07-T03 的断言以迁就偏离实现**（实现不改测试断言是 dev-exe 铁律）。
- 修正锚点：
  - `lib/core/services/audio_source_builder.dart:155` 恢复 spec 裁决的 catch+debugPrint+return。
  - `lib/features/connection/connection_provider.dart:165` 超时改 `WebDavValidationResult.error('读取密码超时，请重试')`（新增工厂+枚举值）。
  - `SecureStorageTimeoutException` 补 timeout 字段/命名参数/toString 对齐 spec。
  - **恢复 BUG-07-T03 原断言**。
- 门禁：`test/features/coverage/bug_bug32_repro_test.dart`（S1/S2/INV1-3，13 用例含否定断言；因 .gitignore `coverage/` 误伤需 `git add -f`，见 O6）。

#### M6. BUG-23 S3 词边界 + S4 3xx/5xx 状态码分离完全缺失

- 类型 / 严重度 / 维度：spec 落地不全 / Major / 功能-错误诊断
- 原修复：193ef56
- 缺陷证据：
  - S3 只加了 responseRegex 大小写不敏感；`_extractXmlContent` 的 `\b` 词边界 + `RegExp.escape` 未实现（NET5 子串误匹配仍在）。
  - S4（NET6）整体缺失：validate 3xx/5xx 仍报「无法连接」、listDirectory 非 207 仍一律裸状态码——把用户指向错误的修复方向。
- 修正锚点：
  - `lib/core/network/webdav_client.dart:231-261` validate 按状态码分离：207/2xx 成功、401/403 认证、404 路径不存在、3xx「服务器重定向，请检查地址是否应为 https」、5xx「服务器内部错误，请稍后重试」、其余兜底 networkError。
  - `_extractXmlContent` 补 `\b` 词边界 + `RegExp.escape`。
- 门禁：`bug_bug23_repro_test.dart` 18 条（MockClient/MockClient.streaming 注入真实 client：body 停滞超时/drain 不翻转/大小写与子串 XML/3xx/5xx/4xx 映射/兜底固定文案，含否定断言 + 日志可追溯性 + 207 回归）。**回退 lib 改动后 5 条判别用例 FAIL**。

#### M7. BUG-22 焦点映射错误致通话结束后无法恢复播放（用户裁决 U2 被压掉）

- 类型 / 严重度 / 维度：语义偏离 / Major / 功能-用户裁决
- 原修复：ee7d1e0
- 缺陷证据：
  - 原映射 `pause→lost` 忽略 `AudioInterruptionEvent.begin`——**通话结束后的焦点恢复事件 `(begin:false, type:pause)` 被当永久丢失再次 pause**，压掉 just_audio 的自动恢复，破坏用户裁决「通话结束恢复」（U2）；且使状态机 `audioFocus` 永久滞留 lost。
  - BUG-18：stream 等待超时 15s，spec S1 明确 30s（ee7d1e0 自改的 aud_02 T07 组名即写 30s，实现与测试自相矛盾）。
  - BUG-31：ee7d1e0 只做了 BRW5（ValueKey），BRW8 时钟注入整体缺失。
- 修正锚点：
  - `lib/core/services/audio_handler.dart:82-99` 映射改 spec 的 `pause+duck→transient`、`unknown→lost`；`_becomingNoisySub` 类型同步为 `StreamSubscription<void>`（spec 原文）。
  - `lib/features/player/domain/playback_orchestrator.dart:228-230` 超时归位 30s；aud_02 T07 改断言分层语义（BUG-05 gate 20s 先于内层 30s 结束请求，内层仍触发 stop 收尾）。
  - `lib/features/browser/domain/directory_service.dart:75-89,109,177` DirectoryService clock 可选参数 + `browserClockProvider`，替换全部 5 处硬编码 `DateTime.now()`（默认行为不变）。
  - pubspec：`audio_session` 从 dev_dependencies 移到 dependencies（lib/ 已直接 import，spec 误记）。
- 门禁：三份 spec §5.4 门禁全补齐——`bug_18_stream_wait_test.dart`（fast path/16s 慢 NAS/超时分层的 stop，INV1 无轮询复活源码扫描）、`bug_bug22_repro_test.dart`（S1 接线/映射 + INV1 无 `_player` 直调 + INV2 dispose 清理静态守卫 + 挂起 pause 5s 超时不挂死）、`bug_bug31_repro_test.dart`（ValueKey 断言 + 注入时钟 TTL 边界 + provider 时钟覆盖确定性过期）。

#### M8. BUG-24/25 spec 仅落地约 1/3，多处修复对目标场景实际无效

- 类型 / 严重度 / 维度：spec 落地不全 + 修复无效 / Major / 功能-崩溃面
- 原修复：3d6bc26（LIST5/LIST7/CON7 已落地）+ 596a63b 夹带
- 缺陷证据（逐项）：
  1. **CON6（BUG-24-S2）update() 原子性回滚完全缺失**：DAO 更新失败后 secure_storage 已写新密码，状态撕裂。
  2. **CON8（BUG-24-S4）测试连接仍用表单空密码**：编辑页密码留空时 validate 必失败。
  3. **CON5（BUG-24-S1）只做半套**：强解包消除但缺按 id 现查兜底；另发现**捕获成功也不 setState**，保存按钮状态冻结在首帧值（`original==null`→门控关闭），S10 只改显示名永不可存——CON-05 起潜伏，CR CON12 零 widget 测试所致。
  4. **LIST3（BUG-25-S1）importPlaylist 残留三处 `as` 强转**：name/filePath/fileName 非字符串抛 TypeError，违反 INV1。
  5. **LIST6（BUG-25-S3）两处 fire-and-forget 无错误处理**。
  6. **LIST7（BUG-25-S4）原修复的 `context.mounted` 检查在 defunct State 上 context getter 本身即抛**（debug FlutterError / release null check）——行 51 检查被 catch 吞后行 72 再抛，门禁实证原修复对目标场景无效。
  7. **LIST8（BUG-25-S5）两对话框 controller 未 dispose**；spec 的「await showDialog 后 dispose」方案与退场动画竞态（内容仍 mounted 即 use-after-disposed 崩溃，门禁实证）。
- 修正锚点：
  - `lib/features/connection/domain/connection_service.dart:89-104` update 补 storage 回滚（读旧密码→写新→DAO 失败回写旧→rethrow，对齐 save() 策略）。
  - `lib/features/connection/connection_edit_screen.dart:247` 空密码回读 secure_storage（BUG-32 后读超时按裁决降级空串）；CON5 捕获成功补 setState + 按 id 现查兜底。
  - `lib/features/playlist/domain/playlist_service.dart:157,159,167,172` `as` 强转改 `is` 检查；tracks 非数组按边界裁决当空列表。
  - LIST6 补 catchError/try-catch + SnackBar + debugPrint（同 BUG-19 复核判据）。
  - LIST7 改 State 级 `mounted`；LIST8 按 CR 首推方案抽 StatefulWidget——`lib/features/playlist/playlist_list_screen.dart:131-148` `_CreatePlaylistDialog`/`_RenamePlaylistDialog` 改 ConsumerStatefulWidget，controller 随对话框元素 dispose。
- 门禁：`bug_bug24_repro_test.dart`（10 例）+ `bug_bug25_repro_test.dart`（14 例），覆盖两份 spec §5.4 全部 S/INV；**对修复前代码实证 FAIL（TypeError / 静默吞错 / defunct 崩溃 / controller 泄漏 / S10 冻结）**。

### Minor

#### m1. CON1 dispose 后保存/更新失败静默无日志

- 原修复：cf1010e（刷新职责下沉、A-5 惰性守卫均忠实，门禁对修复前实证 FAIL）。唯一缺口：页面 dispose 后失败时 widget catch 只有 `if (mounted)` 分支，错误静默丢失。复核判据：**catch 可接受的前提是有日志**。
- 修正锚点：`lib/features/connection/connection_screen.dart:223`、`connection_edit_screen.dart:371` 补 else 分支 debugPrint。门禁 bug_15_repro_test 新增 CON1-C/D（dispose 后 storage 写失败→日志落地 + 回滚无残留/DB 保持旧值 + 不误刷新派生 provider）。

#### m2. BUG-19 catchError((_) {}) 静默吞错

- 原修复：84acb3f（unawaited+catchError 包裹、签名不变 7 处调用方零改动，忠实）。缺口：catchError 无日志，违反 spec §3.1 S1「捕获错误并记日志」。
- 修正锚点：`lib/features/player/domain/playback_orchestrator.dart:413-419` 补 `debugPrint('[Player] saveProgress failed: $e')`。
- 门禁：`bug_bug19_repro_test.dart` 10 例（DB busy / dispose 窗口 in-flight 失败断言无 unhandled async error 且日志落地；7 处调用方回归）。**对修复前代码 FAIL（unhandled error）、对 84acb3f 静默版也 FAIL（日志断言）**。

#### m3. BUG-26 S4 clock 注入整体缺失 + 窄捕获偏离

- 原修复：d5da6c0。缺口：S4（DB6）三个 DAO 仍硬编码 `DateTime.now()`；S3 窄捕获用 `e.toString().contains('no such table')` 偏离 spec 明文裁决的 sqflite 官方 API。
- 修正锚点：`playlist_dao.dart:12-18,46,120`、`progress_dao.dart:19-20`、`connection_dao.dart` 共 5 处构造函数注入 clock（默认 `DateTime.now`，生产行为不变）；窄捕获改 `e.isNoSuchTableError()`。
- 门禁：`bug_bug26_repro_test.dart` 24 用例（含「视图冒充表」错误注入验证窄捕获 rethrow + 事务回滚）。

---

## §3 门禁测试缺失清单（生产修复忠实、测试零覆盖）

> 共性证据：各 spec §5.4 指定门禁文件在 dev-status.json 中 `test_files` 为空、状态 pending；既有用例对修复零锚定——**回退生产修复，全部既有用例照样绿**。每项均按 spec 补齐门禁并完成两态实证（修复前 FAIL / 修复后 PASS）。

| 项 | 原修复 | 零覆盖盲区证据 | 补齐门禁 |
|---|--------|---------------|---------|
| BUG-15 href 识别（f1ce67f） | 8d71b72 | 既有 brw_01/brw_03 夹具 displayname 全部带扩展名（cr MDL2 盲区） | `bug_15_repro_test.dart` 17 例 |
| BUG-28 setSeekStep（9e8a4c6） | 7cc3b05 | `settings_test.dart:344` 只断言 prefs/settingProvider，**从未读 seekStepProvider**（cr SET1 盲区） | `bug_bug28_repro_test.dart` 9 例 |
| BUG-20 清除窗口（cd181dd） | 67025ad | §5.3 要求的 12000/15000/50000/600000 边界全套零覆盖；ref_24/aud_01 仅改写 2 条 | `bug_bug20_repro_test.dart` 23 例 + 修过期注释 |
| BUG-11+12（2555c35） | 4af0fe5 | 无门禁；注意 browser/ 下同名 bug_11/12_repro 是 9cd9ce2 轮另一批 bug（编号复用，见 S5） | `bug_11_test.dart` + `bug_12_test.dart` |
| BUG-07 Tab 菜单（67e810b） | 769f846 | ply_09 PLY-T65 只断言排序图标存在，从未驱动 Tab 切换验证菜单内容 | `bug_07_tab_sort_test.dart` 5 例；撤除 setState 行后 4/5 FAIL |
| BUG-09 跨目录全选（fd8e69a） | 4f50954 | ply_14 TST-T85/T86 仅单目录、TST-T88 跨目录但导航前无选择（cr LIST2 盲区） | `bug_09_test.dart` 5 例；回退后 U1/U2/U3 FAIL |
| BUG-13 删进度（a9221c6） | d4856ec | PRG-T20/FIX-T02/TST-T21 只断言 dialogResult/startPositionMs，**对删除副作用零断言**（cr PRG3 盲区） | `bug_13_repro_test.dart` 5 例走真实生产链路；回退 else-if 后 FAIL |
| BUG-06 下一曲响应（897d356） | b9dbeb7 | brw_09 全部用例 pump 前静态打桩 playing/playingStream（cr BRW11 响应性盲区） | `bug_06_playing_stream_test.dart` 4 例；回退快照实现后 FAIL |
| BUG-29 定时器显示（a89d61b） | b1a4831 | 既有 timer_test 全部以 `noopRemainingTimeOverride` 替换真实流（cr TMR6 盲区），回退 b1a4831 照样绿 | `bug_bug29_repro_test.dart` 14 例；回退后 12 条 FAIL |

（实现缺陷类各项的门禁补齐情况已在 §2 各项内注明：共新增/扩充门禁测试 26 个文件，测试总量 1713 → 1980。）

---

## §4 系统性问题（流程层）

### S1. 门禁链条断裂——dev-exe「只改 lib/ 即标 done」

25 项中 **18 项** spec §5.4 指定门禁测试从未落地；`docs/dev/dev-status.json` 对应条目 `test_files` 全空、状态全 pending。修复验收没有「修复前 FAIL → 修复后 PASS」的实证闭环，导致本轮复核必须逐项补做。
**建议**：dev-exe 脚本门禁强制校验 spec §5.4 指定文件存在且通过 `repro-test.sh` 两态验证后才许标 done；dev-status 的 test_files 字段由脚本从 spec 解析回填，不手填。

### S2. spec 只实现一半

BUG-16/23/24/25/26/31 六项规定多 Scenario，实际只挑容易的做（如 BUG-23 只做大小写不做词边界和状态码分离；BUG-26 不做 clock 注入；BUG-24 三个 S 只做一个半）。
**建议**：dev-exe 完成判定按 spec §3.1 修改指令逐条核对，而非按「能跑通」判定。

### S3. 两条流程红线

1. **篡改既有断言**：8c58a08（BUG-32）为迁就偏差实现改写 BUG-07-T03 断言（已在 08fd938 恢复）。
2. **spec 模式未验证可行性**：BUG-21 的 `ref.read`-in-`onDispose`、BUG-25-S4 的 `context.mounted`、BUG-25-S5 的「await 后 dispose」三个方案在真实 Riverpod/Flutter 语义下均不可行/无效，dev-plan 未验证即写入修改指令。
**建议**：dev-plan 引入新模式/新 API 用法时须在 spec 中标注可行性依据（版本行为文档或最小验证片段）。

### S4. spec 自身实现性错误（需 dev-plan 修订，本轮已按代码为准修正实现）

- BUG-04：spec 附 regenerate 代码片段 `n=1` 时死循环。
- BUG-25-S4/S5：spec 方案被门禁实证不可行（见 M8 第 6/7 条）。
- BUG-18：spec 与 audio_session 依赖位置记录错误（audio_session 实为主依赖，已随 2f946ff 修正 pubspec）。
按 CLAUDE.md「代码为准、文档走 dev-plan」原则，docs/features/ 未动，**待 dev-plan 增量修订**。

### S5. BUG 编号复用冲突

`test/features/browser/bug_11_repro_test.dart`/`bug_12_repro_test.dart` 是 9cd9ce2 轮 PRG2/BRW1 的测试，与新一轮 BUG-11/BUG-12 编号撞名（新轮门禁被迫另命名 `bug_11_test.dart`）。两轮 CR 的 bug 编号空间应隔离（如按 CR 文档前缀）。

### S6. catch-log 纪律需单源化

本轮复核统一执行「catch 可接受的前提是有日志」判据（CON1/BUG-19/LIST6 同标准），但 BUG-32-S1 spec 文字与该判据存在表述张力，导致 8c58a08 与 08fd938 实现来回。**建议**：把「静默吞错禁止；catch 必 debugPrint 或 LogBuffer」写入 SCHEMA.md 或 platform-pitfalls 作为全局裁决，spec 不再逐条重复。

---

## §5 范围外问题（本轮未修，待裁决）

> 以下问题先于本轮修复存在或属未修复 spec，复核 agent 未越界处理。每条带证据，供 dev-plan 排期。

#### O1. NET1 遗留持久化绝对路径无迁移（Major，需 dev-plan 裁决）

- 证据：队列持久化 `lib/features/browser/browser_provider.dart:148`（`prefs.setString(_qKey, jsonEncode(next.toMap()))`，`filePaths` 为绝对路径）、恢复读取 `:170`；`play_progress.file_path`（progress_dao）、`playlist_tracks.filePath`（playlist_dao）同样存绝对路径。
- NET1 修复（ba206e8）只统一了 `validate`/`listDirectory` 的 base path 解析（`lib/core/network/webdav_client.dart:114-123`、`:204-209`），**对修复前已持久化的绝对路径无迁移**——子路径挂载场景下拼接会双重前缀，旧播放单/进度/队列命中 404。
- 建议：dev-plan 裁决迁移策略（读取时归一化 vs 一次性数据迁移）。

#### O2. removeTrack 进度张冠李戴（Major）

- 证据：`lib/features/player/domain/playback_orchestrator.dart:371-374`——删除当前曲时先 `queue = newQueue` 再 `saveProgress()`；而 `saveProgress()`（`:402-413`）读 `q.current.path`（此时已是**下一曲**）+ `player.position`（仍是**被删曲目**的位置）→ 旧曲播放位置被记到下一曲名下，下次播放下一曲会从错误位置恢复。
- 建议：saveProgress 在 queue 重赋值前调用，或显式传入被删曲目的 path。

#### O3. playModeProvider 重启不同步，恢复的 shuffle 休眠（Minor）

- 证据：恢复路径 `lib/features/browser/browser_provider.dart:155-200` 写入 `PlayQueue.fromMap(m, files)`（含 shuffleOrder/shufflePosition）；但 `lib/features/player/player_provider.dart:128` `playModeProvider` 初值恒 `sequential`，且 `:120-121` orchestrator.playMode 从它同步 → 恢复的 shuffle 排列处于休眠态，直到用户手动切模式才生效，与「恢复上次退出状态」预期不符。
- 建议：恢复队列时按 `PlayQueue.mode` 回写 playModeProvider。

#### O4. 未修复 spec：BUG-08（LIST1 播放单乱序）、BUG-27（PLY4+PLY5）

- 证据：`git log 9cd9ce2..8c58a08` 无对应修复提交；dev-status.json 状态 pending。仍待 dev-exe。

#### O5. cr-20260728-1700 的 D1/D2/T1 无动作

- D1：BUG-22 复核修正（2f946ff）已改 `_initAudioSession` 映射（`audio_handler.dart:82-99`），但 `onAudioFocusChange` 的 `gained` 分支（`:214`）仍无任何投递方，死代码+文档不一致残留——需按 D1 原建议二选一裁决（删分支 or 实现自动恢复）。
- D2：`playlist_dao.dart` 两处 ORDER BY 不一致（`:70` vs `:104`），无实际影响但属回归隐患。
- T1：音频会话中断绑定仍无独立测试（2f946ff 的门禁走源码静态守卫 + 状态机单测，未 mock AudioSession 事件流）。
- 另：该报告本身仍未纳入 git（untracked）。

#### O6. .gitignore `coverage/` 误伤测试目录（Info）

- 证据：`.gitignore:24` `coverage/` 无锚定，误伤 `test/features/coverage/`——BUG-26/32 门禁测试需 `git add -f` 才能入库。建议改 `/coverage/`。

#### O7. safeStorageDelete 静默吞错（Minor，同 m1/m2 判据）

- 证据：`lib/features/connection/domain/connection_service.dart:120-122` `try { await safeStorageDelete(...); } catch (_) {}`——删除连接时密码清理失败无日志。与 CON1/BUG-19/LIST6 复核判据（catch 必有日志）同类。

#### O8. PlaylistTrack.toNasFile 的 audioType 同源残留（Info，BUG-15 同根）

- 证据：`lib/shared/models/playlist.dart:107-114` `toNasFile()` 的 `audioType` 由 `fileName`（显示名）派生——播放单导入曲目的显示名可能无扩展名/扩展名不一致，分类失真。BUG-15 只修了浏览链路，播放单链路同根未动。

#### O9. dev-status.json 全面失步（流程）

- 证据：所有已修复条目状态仍 pending、test_files 为空，与实际 git 历史（修复 + 本轮 21 个复核修正提交）完全脱节。与 S1 同根。

#### O10. 超时分层已随本轮对齐（无需动作，留档）

- 现状：BUG-05 平台调用 gate 5s（audio_handler 六方法）、连接验证 gate 20s、BUG-18 内层 stream 等待 30s——分层语义已在 2f946ff/cbb3098 中显式断言。建议把该分层表记入 `docs/dev/platform-pitfalls.md` 防回归。

---

## §6 复核修正提交索引（21 个，均已推送 origin/main）

| 提交 | 内容 |
|------|------|
| ef3305f | CON2/NET7 脱敏正则剥到 authority 末位 @ + userInfo 专属文案 |
| d0f43c4 | CON1 dispose 后失败补日志 |
| 720de22 | BUG-16 S2 测试库单源化 + 重启后 FK 生效门禁 |
| f1ce67f | BUG-15 门禁 |
| 9e8a4c6 | BUG-28 门禁 |
| cd181dd | BUG-20 门禁 + 过期注释 |
| 2555c35 | BUG-11/12 门禁 |
| cbb3098 | BUG-05 seek 不阻塞 play + BUG-05/17 门禁 |
| 67e810b | BUG-07 门禁 |
| fd8e69a | BUG-09 门禁 |
| a9221c6 | BUG-13 门禁 |
| 897d356 | BUG-06 门禁 |
| 0607ee3 | BUG-19 catchError 补日志 + 门禁 |
| ba73f85 | BUG-21 局部句柄取消 + 门禁 |
| 418701d | BUG-26 clock 注入 + isNoSuchTableError + 门禁 |
| a89d61b | BUG-29 门禁 |
| 08fd938 | BUG-32 容错归位 + 恢复 BUG-07-T03 断言 + 门禁 |
| 58d4f11 | BUG-23 词边界 + 状态码分离 + 门禁 |
| 17a9010 | BUG-04 PLY3 重洗 + withIndex/fromMap + BUG-14 build 期守护 + 门禁 |
| 2f946ff | BUG-18 超时 30s + BUG-22 焦点映射 + BUG-31 时钟注入 + 门禁 |
| ef3d386 | BUG-24/25 补缺 + 无效检查修正 + 门禁 |

## §7 合格项说明（4 项）

1. **PRG1/PRG2/BRW 进度簇**：实现与 spec 逐条一致，门禁两态有效。
2. **NET1 basePath 同源**：validate/listDirectory 同走 `resolveWebDavBasePath`，实现正确（遗留数据问题单列 O1）。
3. **CON3 编辑连接清缓存**：容器级 invalidate 完整；顺带证实 CON4（编辑页重验证）已在 596a63b 落地且正确。
4. **BUG-30 modifiedAt**：忠实于 spec，门禁有效。
